controller class ~.web.SendEMail
// Create your own controller, will need to add code

json all
// make all controller use JSON

web flow

// will need in	webmvc-config.xml:
// <bean id="conversionService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean" />
// and change webflow-config.xml accordingly.
